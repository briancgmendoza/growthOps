/**
 * In my current company, we have 50+ sites
 * and all of them uses the same components
 * just different config file like this,
 * I was trying to do the same thing, but I realized
 * its too much work.
 */
export const bannerConfig = {
  title: "GrowthOps Exam",
  className: "fw-bold",
};
